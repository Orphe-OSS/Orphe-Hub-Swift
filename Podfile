# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Orphe-Hub-Swift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'OSCKit'
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'CSwiftV'

  # Pods for Orphe-Hub-Swift

  target 'Orphe-Hub-SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Orphe-Hub-SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
